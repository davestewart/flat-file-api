{
  "name": "flat-file-api",
  "version": "0.2.0",
  "description": "A command line tool to mock APIs using flat files",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js --port 8888 --root api --debug true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.6.1",
    "express": "^4.16.4",
    "get-port": "^4.0.0",
    "glob": "^7.1.3",
    "yargs": "^12.0.5"
  },
  "devDependencies": {}
}
