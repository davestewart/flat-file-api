{
  "name": "mock-api",
  "version": "1.0.0",
  "description": "A file-based node app to mock API calls",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "http": "0.0.0",
    "reload": "^2.3.1",
    "watch": "^1.0.2"
  }
}
